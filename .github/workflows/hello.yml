name: Hello Action with Email

on: [push]  # Triggers on any push to any branch

jobs:
  test:
    runs-on: ubuntu-latest  # Free GitHub runner (virtual machine)
    steps:
    - name: Checkout code  # Step 1: Get your repo files
      uses: actions/checkout@v4  # Reusable action

    - name: 23f2002420@ds.study.iitm.ac.in  # Your required step (Step 2)
      run: echo "Hello from my GitHub Action!"

    - name: List files  # Step 3: Show it worked
      run: ls -la
